public enum InputMaps
{
    BASE,
    DIFFUSE
}
public enum CommonMaps
{  
    HEIGHT,
    NORMAL,
    AMBIENT_OCCLUSION
}
public enum MRMaps
{    
    METALLIC,
    ROUGHNESS
}
public enum SGMaps
{
    SPECULAR,
    GLOSSINESS
}